/* ======================================================================
   PSO URUGUAY - CONFIGURACIÓN GENERAL
   ====================================================================== */

const PSO_CONFIG = {
  // Usuario y contraseña del panel de administración
  ADMIN_USER: "admin",
  ADMIN_PASS: "pso2026",

  // Base de la API. Vacío = misma origin (cuando lo sirve server.js).
  // Si desplegás el front y el server por separado, poné acá la URL
  // del servidor, por ejemplo: "https://api.tudominio.uy"
  API_URL: ""
};